<% provide(:title, "New Route") %>

<div class="form-page">
  <div class="form-header">
    <div>
      <h1>New Route</h1>
      <p>Create a delivery route with departure timing and notes.</p>
    </div>

    <div class="form-header-actions">
      <%= link_to 'Back', routes_path, class: "button small secondary" %>
    </div>
  </div>

  <%= render 'form' %>
</div>
