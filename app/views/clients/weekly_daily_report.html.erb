<% provide(:title, "Generate Weekly/Daily Client Reports") %>

<div class="report-page report-generator-page">
  <%= render "shared/report_header",
    title: "Client Totals",
    date_text: @date.strftime("%A %b. %e, %Y"),
    status: "Daily or weekly" %>

  <div class="report-toolbar">
    <%= simple_form_for "", url: print_weekly_daily_report_clients_path, method: :get, html: { class: "report-print-form", data: { turbo_stream: true } } do |f| %>
      <div class="report-date-form report-date-form-with-actions">
        <%= f.input :date, input_html: { value: @date.strftime("%Y-%m-%d"), class: "js-datepicker", name: :date}, required: false %>
        <div class="report-action-buttons">
          <%= button_tag "#{icon('download')} Export Daily Totals".html_safe, name: "type", value: "daily", class: "button search-button" %>
          <%= button_tag "#{icon('download')} Export Weekly Totals".html_safe, name: "type", value: "weekly", class: "button secondary search-button" %>
        </div>
      </div>
    <% end %>
    <div class="report-toolbar-note">
      <span>Client report</span>
      Choose daily or weekly client totals for the selected date.
    </div>
  </div>

  <%= link_to "Back to Reports", reports_path, class: "button small secondary report-back-link" %>
</div>
