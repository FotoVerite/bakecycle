<% provide(:title, "Editing Client: #{client.name}") %>

<div class="form-page">
  <div class="form-header">
    <div>
      <h1>Editing Client: <%= @client.name %></h1>
      <p>Update billing, delivery, and contact details used across client workflows.</p>
    </div>

    <div class="form-header-actions">
      <%= link_to 'Back', clients_path, class: "button small secondary" %>
    </div>
  </div>

  <%= render 'form' %>
</div>
