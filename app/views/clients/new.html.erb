<% provide(:title, "New Client") %>

<div class="form-page">
  <div class="form-header">
    <div>
      <h1>New Client</h1>
      <p>Create a client record for orders, delivery, billing, and contacts.</p>
    </div>

    <div class="form-header-actions">
      <%= link_to 'Back', clients_path, class: "button small secondary" %>
    </div>
  </div>

  <%= render 'form' %>
</div>
