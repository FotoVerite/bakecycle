<% provide(:title, "Editing Ingredient: #{@ingredient.name}") %>

<div class="form-page">
  <div class="form-header">
    <div>
      <h1>Editing Ingredient: <%= @ingredient.name %></h1>
      <p>Update ingredient details used by recipes and production costing.</p>
    </div>

    <div class="form-header-actions">
      <%= link_to 'Back', ingredients_path, class: "button small secondary" %>
    </div>
  </div>

  <%= render 'form' %>
</div>
