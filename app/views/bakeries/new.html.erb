<% provide(:title, "New Bakery") %>

<div class="form-page">
  <div class="form-header">
    <div>
      <h1>New Bakery</h1>
      <p>Create a bakery workspace with contact, schedule, and account settings.</p>
    </div>

    <div class="form-header-actions">
      <%= link_to 'Back', bakeries_path, class: "button small secondary" %>
    </div>
  </div>

  <%= render 'form' %>
</div>
