<% provide(:title, "New Vendor") %>

<div class="form-page">
  <div class="form-header">
    <div>
      <h1>New Vendor</h1>
      <p>Create a vendor contact for purchasing and supplier records.</p>
    </div>

    <div class="form-header-actions">
      <%= link_to 'Back', vendors_path, class: "button small secondary" %>
    </div>
  </div>

  <%= render 'form' %>
</div>
