<% provide(:title, "Editing Vendor: #{@vendor.name}") %>

<div class="form-page">
  <div class="form-header">
    <div>
      <h1>Editing Vendor: <%= @vendor.name %></h1>
      <p>Update vendor contacts used by purchasing and supplier records.</p>
    </div>

    <div class="form-header-actions">
      <%= link_to 'Back', vendors_path, class: "button small secondary" %>
    </div>
  </div>

  <%= render 'form' %>
</div>
