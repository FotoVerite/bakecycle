<% provide(:title, "New User") %>

<div class="form-page">
  <div class="form-header">
    <div>
      <h1>New User</h1>
      <p>Create a user and assign bakery access.</p>
    </div>

    <div class="form-header-actions">
      <%= link_to 'Back', users_path, class: "button small secondary" %>
    </div>
  </div>

  <%= render 'form' %>
</div>
