<% provide(:title, "New User") %>

<div class="form-page">
  <div class="form-header">
    <div>
      <h1>New User</h1>
      <p>Create a public client user and connect them to a client.</p>
    </div>

    <div class="form-header-actions">
      <%= link_to 'Back', public_client_users_path, class: "button small secondary" %>
    </div>
  </div>

  <%= render 'form' %>
</div>
