<% provide(:title, "Grocery List") %>

<div class="report-page report-generator-page">
  <%= render "shared/report_header",
    title: "Grocery List",
    date_text: @date.strftime("%A %b. %e, %Y"),
    status: "Product runs" %>

  <div class="report-toolbar">
    <%= simple_form_for :future_invoices, url: print_grocery_list_orders_path, method: :get, html: { class: "report-print-form", data: { turbo_stream: true } } do |f| %>
      <div class="report-date-form">
        <%= f.input :date, input_html: { value: @date.strftime("%Y-%m-%d"), class: "js-datepicker", name: :date}, required: false %>
        <%= button_tag "#{icon('download')} Export Grocery List".html_safe, class: "button search-button" %>
      </div>
    <% end %>
    <div class="report-toolbar-note">
      <span>Production report</span>
      Builds the grocery list for product runs on this date.
    </div>
  </div>

  <%= link_to "Back to Reports", reports_path, class: "button small secondary report-back-link" %>
</div>
