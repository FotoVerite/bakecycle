<% provide(:title, "Created Products - #{@date.strftime('%A %b. %e, %Y')}") %>

<h1>Created Products &mdash; <%= @date.strftime("%A %b. %e, %Y") %></h1>

<%= simple_form_for :search, method: :get do |f| %>
  <div class="row collapse">
    <div class="small-12 medium-3 columns">
      <%= f.input :date, input_html: { value: @date.strftime("%Y-%m-%d"), class: "js-datepicker", name: :date}, required: false %>
    </div>
    <div class="small-12 medium-3 columns end">
      <%= f.submit 'Select Date', class: "button postfix search-button", name: nil %>
    </div>
  </div>
<% end %>

<% if @products.any? %>
<%= render partial: "product_table", locals: {
    products: @products.decorate,
    show_created_whodoneit: true,
    show_updated_at_whodoneit: false
  }
%>
<% else %>
  <h2>No Products created for this date.</h2>
<% end %>
